package controllers

import (
	"github.com/kumail-sys/go-bookStore/pkg/models"
	"net/http"
)

var NewBook models.Book

func GetBook(w *http.ResponseWriter, r *http.Request) {
	//newBooks := models.GetBook()
	//res, _ := json.Marshal(newBooks)
	
}
